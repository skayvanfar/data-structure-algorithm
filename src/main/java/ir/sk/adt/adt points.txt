Wrapper adts:
Data-structure   --->   Wrapper-adt
Array            --->   List
LinkedList       --->   Deque
HeapTree         --->   PriorityQueue
Hashtable        --->   Dictionary

array representation of a tree: putting the nodes in level order

map.get(key) == set.contains(item)
traverse belongs to data-structure vs search(get, contains) belongs to adt

data structure implementation when  need    search,     add (common adts like set, map)
unordered array                             O(n),       O(n)
ordered array                               O(Long n)   O(n)
link list                                   O(n)        O(1)
bst                                         O(Long n)   o(Log n)
hashtable                                   O(1)        o(1)